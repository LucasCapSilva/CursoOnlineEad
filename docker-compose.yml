
version: '3.3'
services:
  front-end:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - generation-network 
networks:
     generation-network:
       driver: bridge
